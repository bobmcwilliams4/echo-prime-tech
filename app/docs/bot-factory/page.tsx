'use client'

import ProductDoc from '@/components/ProductDoc'

const data = {
  name: 'Echo Bot Factory',
  tagline: 'Automated bot creation and lifecycle management across 8 platforms. From template to deployed bot in minutes — with AI-generated content, vault-secured credentials, cron scheduling, and engagement analytics.',
  accent: '#f97316',
  productUrl: '/bots',
  workerUrl: 'https://echo-bot-factory.bmcii1976.workers.dev',
  version: '2.4.0',
  overview: [
    'Echo Bot Factory is the automated platform for creating, deploying, and managing bots at scale across every major social and communication platform. Whether you need a Discord community moderator, a Telegram customer service agent, a Reddit content monitor, a LinkedIn outreach bot, an X/Twitter engagement driver, an Instagram story poster, a Slack workflow automator, or a WhatsApp broadcast channel, Bot Factory spins them up from battle-tested templates in minutes. Each bot is fully configured with platform credentials stored in the vault, scheduled content via cron, AI-generated posts from the Engine Runtime, and behavioral analytics piped to the Echo analytics pipeline.',
    'The platform operates on a template-and-instance model. Templates define the bot archetype: what platform it runs on, what content strategy it follows, which of the 14 AI personalities it embodies, what its posting schedule looks like, and how it handles incoming interactions. Instances are deployed bots derived from a template — you can run 50 LinkedIn outreach bots from one template, each with different personas, target audiences, and credential sets. Templates and instances are version-controlled so a personality update to the template can be rolled out to all instances simultaneously or tested on a canary subset first.',
    'OPSEC compliance is built into the factory at every layer. Each bot instance uses a dedicated, rotated API token stored in the Echo Vault rather than a shared credential. Posting rate limits are enforced per platform according to the latest published limits, with adaptive throttling when platforms signal congestion. Content generated by AI personalities passes through a compliance filter before posting. Bot activity logs are retained for 90 days with full audit trails showing what was posted, when, from which bot, using which credentials, and what engagement it received.',
  ],
  gettingStarted: [
    { step: 1, title: 'Choose a Bot Template', desc: 'Navigate to /bots to browse the template library. Templates are organized by platform and function (moderator, content poster, engagement driver, customer service, monitoring bot). Each template shows example output, typical engagement rates, and the default AI personality.' },
    { step: 2, title: 'Configure Your Bot Instance', desc: 'Call POST /bots/create with your chosen template_id, bot name, target platform, persona configuration, and posting schedule. Specify the content domain so Engine Runtime generates on-topic posts (e.g., domain: "oilfield" or domain: "tax" or domain: "security").' },
    { step: 3, title: 'Store Platform Credentials', desc: 'Add your platform API credentials via the Vault API before deploying. Bot Factory retrieves credentials by bot_id at runtime. Never pass tokens in the API request body. The factory validates credential scope against the platform\'s required permissions before deployment.' },
    { step: 4, title: 'Deploy and Activate', desc: 'Call POST /bots/:id/deploy to activate the bot. For content-posting bots, this registers the cron schedule and fires the first content generation cycle. For interactive bots (Discord, Telegram, WhatsApp), this connects the webhook listener and verifies platform handshake.' },
    { step: 5, title: 'Monitor and Optimize', desc: 'Check GET /bots/:id/analytics after 24 hours to review engagement rates, posting success rate, rate-limit events, and content performance. Adjust posting frequency, personality tone, or content domain based on what the analytics show is resonating.' },
  ],
  features: [
    { title: 'Template-Based Bot Creation', desc: 'Library of 80+ battle-tested bot templates covering every major platform and use case. Templates encode platform best practices, optimal posting windows, engagement-maximizing content formats, and proven OPSEC patterns. New templates are added as platforms evolve and new patterns are discovered.' },
    { title: 'Multi-Platform Deployment (8 Platforms)', desc: 'Native integration with Discord, Telegram, Slack, Reddit, LinkedIn, X/Twitter, Instagram, and WhatsApp. Each platform adapter handles authentication flows, API quirks, rate limit specifics, and content format requirements transparently. One template definition deploys across multiple platforms simultaneously.' },
    { title: 'Vault-Secured Credential Management', desc: 'All platform tokens, API keys, and session credentials are stored in the Echo Vault with per-bot AES-256 encryption. Credentials are rotated on configurable schedules. Bot Factory never logs or transmits credentials in plaintext — they are loaded at runtime via vault binding and discarded after use.' },
    { title: 'Cron-Based Scheduled Posting', desc: 'Configure posting schedules with cron expressions down to per-minute precision. Schedules are stored in the Bot Orchestrator\'s D1 database and executed via Cloudflare Cron Triggers. Optimal posting windows per platform are pre-computed from engagement analytics and suggested during setup.' },
    { title: 'AI Content Generation via Engine Runtime', desc: 'Content for each post is generated by the Engine Runtime using the configured domain and personality. Domain-grounded posts cite real information (tax law updates, oilfield regulations, security advisories) rather than generic filler. Content passes through a quality gate before scheduling — posts below the quality threshold are regenerated.' },
    { title: '14 AI Personalities', desc: 'Every bot can be assigned one of 14 AI personalities: Strategic Expert, Industry Insider, Thought Leader, Community Builder, Technical Authority, News Analyst, Provocateur, Educator, Storyteller, Data Analyst, Visionary, Challenger, Advisor, or Conversationalist. Personalities shape tone, vocabulary, posting style, and engagement behavior.' },
    { title: 'Bot Health Monitoring', desc: 'Continuous health checks on every deployed bot: credential validity, webhook connectivity, posting success rate, error rate, rate-limit headroom, and last-activity timestamp. DEGRADED bots trigger notifications. CRITICAL bots (silent for > 2 posting cycles) trigger automatic credential refresh and reconnect attempts.' },
    { title: 'Engagement Analytics', desc: 'Per-bot engagement metrics: impressions, likes, shares, comments, clicks, follower growth, reply sentiment, and top-performing posts. Analytics are aggregated across bot instances so you can compare which personas perform best on each platform and refine your strategy.' },
    { title: 'A/B Testing for Content', desc: 'Create content variants — same topic, different angles, personalities, or posting times — and run A/B tests across bot instances. Bot Factory routes equal traffic to each variant, measures engagement differential over a configurable test window, and automatically promotes the winner.' },
    { title: 'Rate Limit Management', desc: 'Each platform adapter enforces the current published rate limits with a 20% safety margin. Rate limit counters reset per the platform\'s window (usually hourly or daily). If a bot approaches its limit, posting is paused and scheduled posts are rescheduled rather than dropped. Adaptive throttling responds to 429 responses in real time.' },
    { title: 'OPSEC Compliance', desc: 'Per-bot posting delays with configurable jitter to prevent pattern detection. Human-like typing simulation on interactive platforms. Credential rotation on configurable schedules. IP diversity via Cloudflare\'s distributed edge. Anomaly pattern detection to catch and pause bots that are behaving in ways likely to trigger platform suspicion.' },
    { title: 'Bot Auditing', desc: 'Complete audit log for every bot action: content posted, timestamp, platform response, engagement received, errors encountered, credential used, and person or process that triggered the action. Audits are tamper-evident and retained for 90 days. Exportable for compliance review or post-mortem analysis.' },
  ],
  apiEndpoints: [
    { method: 'POST', path: '/bots/create', desc: 'Create a new bot instance from a template. Accepts template_id, bot_name, platform, persona config (personality, tone, domain), posting schedule (cron expression), and target audience definition. Returns bot_id and initialization status. Credential setup must be completed before deploy.', auth: true },
    { method: 'GET', path: '/bots', desc: 'List all bot instances with current status, platform, personality, posting schedule, last post timestamp, and key health metrics (success rate, error count, rate-limit events in last 24h). Supports filtering by platform, status, and personality type.', auth: true },
    { method: 'GET', path: '/bots/:id', desc: 'Get full detail for a specific bot: configuration, template lineage, credential references (names only, never values), complete posting history with engagement data, health metric time series, active A/B tests, and audit log excerpt.', auth: true },
    { method: 'POST', path: '/bots/:id/deploy', desc: 'Activate a configured bot. Validates credentials exist in vault, confirms platform connectivity, registers cron schedule, and starts the bot. For interactive bots, registers the webhook. Returns deployment status and first scheduled post time.', auth: true },
    { method: 'POST', path: '/bots/:id/schedule', desc: 'Update a bot\'s posting schedule without redeployment. Accepts new cron expression, time zone, and optional blackout windows (periods when the bot should not post). Schedule changes take effect on the next cron cycle.', auth: true },
    { method: 'GET', path: '/bots/:id/analytics', desc: 'Retrieve engagement analytics for a bot over a specified time range. Returns per-post metrics (impressions, engagement rate, sentiment score) and aggregate statistics (follower growth, posting success rate, top content categories, best performing post times).', auth: true },
    { method: 'DELETE', path: '/bots/:id', desc: 'Deactivate and delete a bot instance. Cancels cron schedule, disconnects webhooks, revokes platform tokens via the platform API, removes credential references from registry (vault entries are preserved per vault retention policy), and archives the bot record in cold storage.', auth: true },
    { method: 'GET', path: '/health', desc: 'Bot Factory health check. Returns service status, total registered bots, active/paused/degraded/critical counts, posting success rate over the last hour, and any platform API availability issues detected. No authentication required.', auth: false },
  ],
  userGuide: [
    { id: 'template-system', title: 'The Template System', content: [
      'Templates are the DNA of every bot. A template defines the platform target, the interaction model (content poster, responder, monitor, or hybrid), the content strategy (topic domain, tone, format preferences, linking strategy), the engagement rules (when to reply, what triggers a response, which users to engage with), and the default AI personality. Templates are versioned — when you update a template, existing instances can be migrated to the new version or remain on their current version.',
      'The template library is organized into families: Content Amplification (posting, repurposing, scheduling), Community Management (moderation, engagement, welcoming), Customer Engagement (support, lead qualification, FAQ response), Intelligence Gathering (monitoring competitor mentions, tracking keyword sentiment, flagging emerging topics), and Outreach (lead generation, networking, recruitment). Most deployments combine templates from multiple families.',
      'Custom templates can be created from scratch or by forking an existing template. The template editor accepts a JSON configuration document covering all template parameters. Custom templates are stored in your Bot Factory account and can be shared with other users or kept private. After deploying a custom template successfully, ingest it back to the template library so future deployments benefit from your tuning.',
    ]},
    { id: 'ai-personalities', title: 'AI Personalities and Content Generation', content: [
      'Each of the 14 AI personalities has a distinct voice profile: vocabulary level, sentence length distribution, preferred content formats (threads vs. single posts, data-heavy vs. anecdotal), engagement style (provocative questions vs. authoritative statements), and emotional tone (warm and encouraging vs. analytical and dry). Personalities are applied at the content generation layer — the same Engine Runtime domain query produces different post content depending on which personality is active.',
      'Content generation flows as follows: the bot\'s cron fires, Bot Factory calls Engine Runtime with the configured domain query and personality tag, Engine Runtime returns a domain-grounded content brief with source material, Bot Factory applies the personality formatter to produce platform-native content (Tweet thread, LinkedIn article, Reddit post, etc.), the compliance filter checks for prohibited content, and the post is queued for scheduling.',
      'Personality tuning is available at the instance level. You can override the template\'s default personality parameters to make a specific bot instance more formal, more data-focused, or more aggressive in its engagement style. Fine-grained personality overrides include formality_score (0-10), data_citation_frequency (always/sometimes/never), engagement_aggression (passive/moderate/active), and emoji_usage (none/minimal/moderate).',
    ]},
    { id: 'credential-security', title: 'Credential Security and Rotation', content: [
      'Never store platform tokens in the Bot Factory configuration database. All credentials must live in the Echo Vault. Before deploying a bot, store the required credentials with the key pattern bot/{bot_id}/{platform}/token using the Vault API. Bot Factory validates that all required credential keys exist before allowing deployment to proceed.',
      'Credential rotation is automated for platforms that support token refresh flows. LinkedIn, X/Twitter, and Instagram tokens are refreshed before expiry using OAuth refresh tokens. Discord and Telegram bot tokens are long-lived but Bot Factory monitors for platform-reported token invalidation and triggers a re-authentication flow. For platforms where automatic refresh is not possible, Bot Factory sends a notification 7 days before token expiry.',
      'Credential isolation is enforced at the bot instance level. Each bot instance has its own dedicated platform account and token — shared credentials across bot instances are not permitted. This isolation limits blast radius if a platform account is suspended: only that instance goes offline, not the entire fleet of bots using the same credential.',
    ]},
    { id: 'analytics-optimization', title: 'Analytics and Campaign Optimization', content: [
      'Bot Factory analytics are organized into three tiers. Post-level metrics (per-post impressions, engagements, clicks, shares) are available in real time via the Cloudflare Analytics pipeline. Bot-level aggregates (daily reach, follower growth rate, average engagement rate, top content categories) are computed hourly and stored in D1. Account-level cross-bot analytics (which platform is generating the most conversions, which personality is highest-engagement, which content domain is trending) are computed nightly and available in the analytics dashboard.',
      'A/B test setup requires at minimum two bot instances with different configurations and a shared control group definition (same target audience, same time windows, same posting frequency). Bot Factory randomly assigns incoming opportunities to each variant and accumulates statistical significance before declaring a winner. Tests require a minimum of 100 engagement events per variant before results are considered reliable.',
      'The optimization loop: run a bot for 7 days, review analytics for top and bottom performing posts, adjust the content domain focus toward what performed best, test a new personality or tone variant via A/B test, promote the winner to your full bot fleet. This loop compounding over 30-60 days typically doubles engagement rates from initial deployment baselines.',
    ]},
    { id: 'opsec-compliance', title: 'OPSEC and Platform Compliance', content: [
      'Operate within platform terms of service to maximize account longevity. Bot Factory\'s OPSEC module enforces several key behaviors by default: posting rate limits are set 20% below published limits (never test the ceiling), each bot post includes a configurable random delay (default 30s-5min jitter) to avoid robotic posting patterns, and interactive bots insert human-like response latency (2-15 seconds depending on platform norms).',
      'Platform detection evasion is a continuous cat-and-mouse game. Bot Factory\'s OPSEC profiles are updated when platforms change their detection heuristics (platform ban waves often signal a heuristic update). The evasion module tracks the following signals: posting time regularity (variance injection), content repetition ratio (cross-bot content similarity enforcement), engagement pattern distribution (like/follow/comment ratios within platform norms), and session duration patterns.',
      'Compliance logging is mandatory and cannot be disabled. Every bot action is logged with enough detail to answer the question "what did this bot do, when, and why?" for any audit or investigation. Compliance logs are separate from engagement analytics and retained independently. If a bot is suspended by a platform, the compliance log is the first resource for root cause analysis and appeal documentation.',
    ]},
  ],
  aiCapabilities: [
    { capability: 'Domain-Grounded Content Generation', desc: 'Engine Runtime produces content grounded in real domain knowledge — tax law updates, oilfield regulation changes, security vulnerability advisories, financial market events. Posts cite real information, not fabricated facts. Content quality scores above 0.75 are required before a post is scheduled.' },
    { capability: 'Personality-Adaptive Writing', desc: 'NLP models adapt generated content to each personality\'s voice profile. The same factual content becomes a provocative question under the Provocateur personality, a data-rich breakdown under the Data Analyst personality, and a narrative case study under the Storyteller personality. Adaptation is applied post-generation, not at the Engine Runtime query level.' },
    { capability: 'Engagement Prediction', desc: 'Before scheduling, a gradient boosting model predicts the expected engagement rate for each post based on content topic, format, posting time, bot\'s historical performance, and platform-wide trending signals. Low-predicted-engagement posts are flagged for human review or regeneration.' },
    { capability: 'Sentiment Monitoring', desc: 'Replies and mentions received by bots are sentiment-scored in real time. Sustained negative sentiment (> 40% negative over 6 hours) triggers an automatic posting pause and human notification. Positive sentiment spikes trigger increased posting frequency to capitalize on momentum.' },
    { capability: 'Optimal Timing Intelligence', desc: 'Posting time optimization uses a multi-armed bandit algorithm that continuously tests different posting windows and allocates more posts to the windows that historically produce the highest engagement rate for each platform and content domain combination.' },
    { capability: 'Bot Anomaly Detection', desc: 'Behavioral anomaly models flag bots that are acting outside their configured parameters — unexpected posting bursts, unusual content patterns, engagement rate spikes or drops that do not correlate with content quality. Anomalies may indicate credential compromise, platform behavior changes, or template bugs.' },
  ],
  troubleshooting: [
    { issue: 'Bot deployment fails with "Credential not found in vault"', solution: 'You must store platform credentials in the Echo Vault before deploying. Use the Vault API with key pattern bot/{bot_id}/{platform}/token. Run GET /bots/:id to see which credential keys are missing. The deployment preflight check lists all required keys and which ones are present.' },
    { issue: 'Bot is ACTIVE but not posting on schedule', solution: 'Check the cron schedule syntax via GET /bots/:id — invalid cron expressions cause silent failures. Verify the Cloudflare Cron Trigger was registered (check Fleet Commander for the bot Worker). Review the posting log for the last 24 hours via GET /bots/:id/analytics to see if posts were attempted and failed or were not attempted at all.' },
    { issue: 'Content quality gate rejecting all generated posts', solution: 'The quality gate minimum score is 0.75 by default. If Engine Runtime cannot find enough doctrine blocks for the configured domain, scores will be low. Try broadening the content domain (e.g., "oilfield regulations" → "energy industry") or lowering the quality threshold temporarily via PATCH /bots/:id/config. Also verify Engine Runtime is healthy via its health endpoint.' },
    { issue: 'Bot platform account suspended', solution: 'Immediately call DELETE /bots/:id to deactivate the bot and prevent further automated posting. Download the compliance log for the account to review what triggered the suspension. Check the OPSEC compliance report for rate limit violations or content policy flags in the days before suspension. Before re-creating the bot, review and tighten the OPSEC configuration and ensure content complies with platform policies.' },
    { issue: 'A/B test showing no statistical difference after 2 weeks', solution: 'Insufficient sample size is the most common cause. Check the test stats via GET /bots/:id/analytics — each variant needs 100+ engagement events for reliable results. If volume is too low, increase posting frequency. If volume is adequate but difference is genuinely absent, the variable being tested may not matter for this audience — try a more distinct variable (completely different personality or domain instead of minor tone adjustments).' },
  ],
  faq: [
    { q: 'How many bots can I run simultaneously?', a: 'There is no hard limit on the number of bot instances. Each bot runs as a lightweight Cloudflare Worker Cron Trigger with minimal idle cost. Practical limits are set by your platform account limits (number of API tokens, posting accounts), your vault storage capacity, and your analytics query volume. Deployments of 500+ simultaneous bots are supported.' },
    { q: 'Can bots interact with each other to simulate organic conversation?', a: 'Yes. Define a bot network with cross-bot interaction rules: Bot A posts original content, Bot B replies from a supporting perspective within 2-6 hours, Bot C amplifies with a share within 24 hours. Interaction rules are configured in the template network definition. Cross-bot activity is logged as a coordinated campaign rather than as independent bot activity.' },
    { q: 'What AI model generates the bot content?', a: 'Content is generated by Echo Engine Runtime, which uses the appropriate domain-trained adapter for the configured content domain. Tax content uses the TX adapter cluster. Security content uses the CYBER adapter cluster. For general business content, the AI Orchestrator routes to the highest-quality available free provider. No content is generated by generic ChatGPT or Claude calls without domain grounding.' },
    { q: 'How does Bot Factory handle platform API changes?', a: 'Platform adapters are versioned and updated independently of bot instances. When a platform changes its API (Twitter v2, Instagram Graph API updates), the Bot Factory platform adapter is patched and deployed via Fleet Commander. Bot instances continue operating on the existing adapter version until migrated. Migration is automatic for non-breaking changes and requires a brief redeploy for breaking API changes.' },
    { q: 'Can I use my own AI-generated images in bot posts?', a: 'Yes. Configure an image_source in the bot template: either a pre-populated R2 bucket of approved images, an image generation endpoint (DALL-E, Stable Diffusion via Echo engine), or a curated RSS-to-image pipeline. Images are fetched at post time and attached to the post payload. Bot Factory handles platform-specific image format and size requirements automatically.' },
    { q: 'Is there a way to test a bot configuration before going live?', a: 'Yes. Set mode: "dry_run" in the deploy request. In dry-run mode, the bot executes all logic — content generation, scheduling, platform API calls — but all API calls to the actual platform are intercepted and logged rather than sent. The dry-run report shows exactly what would have been posted and when, including content quality scores and predicted engagement rates.' },
  ],
}

export default function BotFactoryDocPage() {
  return <ProductDoc {...data} />
}
