# Immortality Vault V3 — Remediation Matrix (living)

> Build #24413. Findings from [`P0_AUDIT.md`](./P0_AUDIT.md). Each row is closed only with **executable evidence**
> (a passing test / live probe), not a code-written claim. Updated as phases land.

| ID | Finding | Fix | Owner phase | Status | Evidence |
|----|---------|-----|-------------|--------|----------|
| F1 | Systemic BOLA/IDOR — ~70 unauth endpoints; `viewer` is caller-supplied | `vault_auth.py` deny-by-default middleware enforces Firebase token + ownership on every sensitive route; forces bloodline `viewer`=verified uid; owner-signed HMAC `?tok=` media URLs; frontend attaches `Authorization: Bearer <idToken>` on all calls | **P1** | **CLOSED 2026-07-25** | **9/9 IDOR pytest green** (`tests/test_idor.py`); **live prod**: `/memories/{uid}` & `/bloodline/{uid}/tree` no-token → **401** (were open), `/stats`→200 & `/listen/{bad}`→404 (public intact), service active/no-crash. Backend `5dbf1e3`, frontend `43331f8`, `VAULT_AUTH_ENFORCE=1` on :8162. Residual: live logged-in-owner E2E not browser-confirmed (low risk — firebase verify proven in prod consent/video routes; instant rollback armed) |
| F2 | Plaintext training corpus `data/training/<user>/sft.jsonl` reverses DB encryption | Builder writes IVENC2-encrypted `sft.jsonl` (path-bound AAD); trainer decrypts to `/dev/shm` (0600) + shreds in a `finally`; 8 legacy files migrated in-place | P2 | **CLOSED 2026-07-25** | disk-scan `grep -rIl "favorite phrases\|the memories you were trained on\|system prompt" data/training` → **no matches**; all 8 files begin `IVENC2\x00`; trainer decrypt+shred proven. Backend `e3d900f`, manifest `/home/forge/backups/…f2_sft_manifest…json` |
| F3 | Plaintext voiceprints `echo-tts-v2/refs/*.wav` (44 clips) | `ref_crypto.materialize_ref` decrypt-on-load shim (plaintext = proven no-op; IVENC2 → 0600 tmpfs decrypt, cached) wired into `tts_v2_server.py` 3 load sites; encrypt refs in-place after a green-synth baseline | P2 | **STAGED — BLOCKED** | shim deployed backward-compatibly (`.pre_f3_bak`); shadow-encrypted all 44 → decrypted through shim **byte-identical (44/44, 0 failures)**; live cutover **deferred**: pre-existing `echo-convai-tts` model-load outage (HF offline-cache miss, all synth 500) makes the mandatory green-synth gate unsatisfiable. **Blocked on the separate TTS outage** (see queue incident). Live refs untouched (0 encrypted). |
| F4 | `VAULT_STRICT` unset → plaintext-passthrough downgrade | Set `VAULT_STRICT=1` after confirming no plaintext served-media remains; `decrypt_bytes()` now rejects non-`IVENC2` bytes | P2 | **CLOSED 2026-07-25** | pre-flip scan of `data/{videos,voice_samples,bloodline_records,training}` → **0** non-IVENC2 files; `VAULT_STRICT=1` live on :8162; post-flip `/health`→200, no-token→401 (enforcement intact). Rollback: remove `VAULT_STRICT` from `.env.secrets` + restart |
| F5 | Plaintext DB cols `family_records.ocr_text`, `personality_traits.evidence`; unwired `eternal_messages.message_text`, `chat_messages.content`, `biometric_captures.data_json` | All 6 write sites routed through `vault.pgp_encrypt_text`; `db_crypto.decrypt_known_fields` extended to auto-decrypt on read; backfilled existing rows | P2 | **CLOSED 2026-07-25** | backfill: `family_records.ocr_text` 21→0 plaintext, `personality_traits.evidence` 2→0 plaintext; round-trip decrypt proven; pg_dump backup `/home/forge/backups/…f5_pre…dump`. Backend `e3d900f` |
| F6 | Decrypted break-glass export to HAMMER; legacy `cf_echo_immortality_vault` + R2 `r2_key` | Verify+purge dead legacy CF schema; confirm break-glass doesn't export biometric media | P2/P6 | **CLOSED 2026-07-25** (vault scope) | legacy `cf_echo_immortality_vault` verified dead (0 code refs, 0 pg_depend) → pg_dump backup (restorable) → `DROP SCHEMA … CASCADE`; `r2_key` = local paths (misnomer, inside crypto boundary), no CF client; break-glass copies **master_vault.db only** — **no biometric media leaves FORGE**. Follow-up (non-vault): HAMMER-side ACL on the master-vault copy + fleet master-vault at-rest encryption |
| F8 | IVENC2 integrity + key lifecycle unproven | Key-rotation entry-point + tamper/swap/truncation tests | P2 | **CLOSED 2026-07-25** | `vault_crypto` rotation API added; `test_crypto_rotation.py` **5/5** (old-key blobs decrypt post-rotation, new writes use new key), `test_crypto_tamper.py` **6/6** (byte-flip/tag-flip/cross-path-swap/truncation all fail-closed). Backend commit on FORGE |
| F7 | Consent split-brain + no consent gate on text/bloodline ingestion | Single authoritative consent store; gate text/bloodline ingestion of third-party subjects | P1/P4 | OPEN | pending: unauth/ungated ingestion test fails closed |
| C1 | Live public copy "family-controlled / never used to train anything" is untrue | Fix behavior (F1,F2) then correct copy: subject data trains only that subject's own authorized model with explicit consent | P4/P10 | OPEN | pending: copy matches proven behavior |

**Legend:** OPEN → IN-PROGRESS → CLOSED (with evidence link). No row moves to CLOSED on a self-report.

## P3 — capture & interview (progress)

| ID | Item | Status | Evidence |
|----|------|--------|----------|
| P3-prov | Derived memories link to encrypted evidence (KEY acceptance) | **CLOSED 2026-07-25** | `answer_question` sets `memories.interview_id`+`evidence_video_id`, back-links `video_recordings.interview_id`; chain queryable both ways; 4 legacy backfilled, 0 unlinked; `test_provenance_dedup.py` 3/3 |
| P3-dedup-be | Capture dedup infra (idempotency) | **CLOSED 2026-07-25** (server half) | `idempotency_key` + partial-unique idx on interviews/memories/video_recordings; `/answer` + `/video/upload` accept it → `ON CONFLICT DO NOTHING`, idempotent 200; client-key wiring = slice 2 |
| P3-resumable | RAM-only recordings lost on interruption | **CLOSED 2026-07-25** | Backend `/video/upload/{uuid}/chunk`+`/status`+`/finalize` (tmpfs-staged RAM-only, IVENC2 at finalize, idempotent via capture_uuid, TTL sweep) — `test_resumable_upload.py` **6/6** + 12/12 regression. Frontend IndexedDB durable chunk buffer + resumable uploader (resume-via-/status, backoff, never drop on fail) + SW Background Sync. Deployed to prod: pages 200, `/sw.js` 200, `/finalize` 401 (registered+enforcing). Backend `c60349c`, frontend `4263ad3` |
| P3-edit | No user edit/correction of memories (no PUT), transcripts not persisted, no timeline | **CLOSED 2026-07-25 (LIVE)** | Backend `b81912f`: `PUT /memories/{id}` → append-only `vault.memory_corrections` (encrypted), original evidence-of-record **byte-unchanged** (proven); `POST /video/{id}/transcription` (encrypted); `GET /timeline/{uid}` (owner-authed, linked to evidence, `derived:true`). `test_p3_slice3.py` 6/6 + 18/18 regression. Frontend `c9d135b` (inline edit + reveal-original + timeline view, tsc clean) deployed to prod (pages 200). PWA offline via slice-2 SW. **P3 COMPLETE** |

## P4+ (progress)

| ID | Item | Status | Evidence |
|----|------|--------|----------|
| P4-train | Model training without consent / cross-subject leakage / base impersonation | **BACKEND CLOSED 2026-07-25** | `vault.training_consent` gate (build_dataset 403 without active consent; deceased/incapacitated need human-verified legal authority); per-subject path-guarded isolation + zero-leakage test; `personality_registry` registration; `/chat` routes to subject's OWN adapter or **honest refusal** (base never impersonates); disable/retrain/delete lifecycle removes serving+artifacts+consent (audited). `test_p4_personas.py` 9/9 + full suite **44 passed**. Backend `7bcc5a5`. FE persona/consent UI pending |
| P4-voice | Subject voice cloning/synth | **BLOCKED** | Depends on TTS incident #24546 (echo-convai-tts model won't load). `voice_id` left NULL; F3 voiceprint shim staged. Resume after #24546 |
| CAPS | SDK caps for the vault | **DONE (LIVE)** | `echo.immortality.health` + `echo.immortality.stats` (tier 0, aggregate-only, verified invoking live). Per-user private endpoints deliberately NOT exposed (sovereign-key would reopen IDOR) |
| P5-graph | Grounded retrieval / annotations / memorial | **BACKEND CLOSED 2026-07-25** | `POST /memory-chat/{uid}` labels sourced_fact/family_recollection/inference w/ real citations, refuses unsupported; `vault.family_annotations` (encrypted, attributed); `POST /memorial/{uid}` deterministic+reproducible (content_sha256+source_manifest) + consent-scoped (`memorial_consent`). `test_p5_memory_graph.py` 9/9 + full suite **53 passed**. Backend `5e586b1`. FE in flight |
| DOCS | README + privacy arch + data inventory + runbooks | **DONE** | 8 docs `4e62c25` (pushed): README, PRIVACY_ARCHITECTURE, DATA_INVENTORY, runbooks (key-rotation/backup-restore/incident/consent/operator). TODOs: confirm media-backup coverage + PITR (→ P8) |
| P6-lifecycle | Machine-readable export + VERIFIABLE deletion (KEY) | **CLOSED 2026-07-25 (LIVE)** | Backend `1ce25c8`: `GET /export/{uid}` validates vs DATA_INVENTORY (valid:true); `POST /account/{uid}/delete` single-txn across **39 stores** + media/training/adapters + persona + receipt (`vault.deletion_receipts`); **independent probe confirms gone**. `test_p6_lifecycle.py` 8/8. Frontend `3b20f7c` (export download + type-DELETE + receipt view) deployed. Full suite 61/61 |
| P8-dr | Reliability / observability / DR | **CLOSED 2026-07-25** | `vault_p8.py`: privacy-safe JSON access log (hashed uids, never content/tokens) + redaction + generic exception handler; `GET /health/deep` degrades gracefully (TTS/gate down ≠ capture broken); upload quotas + magic-byte/malware validation (denylist+allowlist) on all upload routes. Backups verified: media in nightly restic (**restore-drill decrypt round-trip PASS**), Postgres+DEK covered; PITR OFF documented (RPO~24h). `test_p8_resilience.py` 8/8 + full suite **69/69**. Backend `7956151` |
| P10-copy | "never used to train anything" was untrue (C1) | **FIXED (deploy pending)** | `page.tsx` copy corrected `40ebf13`: subject data trains only that person's own AI likeness, with explicit consent, never any other model. Deploys in P10 final |
| P9-cert | Independent security+privacy certification | **IN PROGRESS** | adversarial reviewer, real evidence, default BLOCK: IDOR/auth-bypass/prompt-injection/model-exfil/path-traversal/malicious-upload/ciphertext-swap/replay/rate-abuse/log-leakage |
